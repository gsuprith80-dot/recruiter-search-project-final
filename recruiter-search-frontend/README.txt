Recruiter Search Frontend (Vite + React)

Quick Start:
1. cd recruiter-search-frontend
2. npm install
3. npm run dev

The frontend calls the backend at http://localhost:4000/api/search by default.
For production builds, run `npm run build` and serve the `dist/` folder with your preferred static server (nginx, etc.).
